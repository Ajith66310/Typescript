
let username : string = "ajith k v"
let age : number = 10
let isusername : boolean = true

const big : bigint = 2n ** 63n -1n

// Operator '+' cannot be applied to types 'bigint' and 'number'.
// const add = big + age

const token : unique symbol = Symbol('token')